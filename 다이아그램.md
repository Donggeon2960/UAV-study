### 🎮 채널별 모터 반응 및 물리적 원리

```mermaid
graph LR
    %% 스타일 정의
    classDef thrust fill:#e3f2fd,stroke:#1565c0,stroke-width:2px;
    classDef roll fill:#fff3e0,stroke:#ef6c00,stroke-width:2px;
    classDef pitch fill:#f3e5f5,stroke:#8e24aa,stroke-width:2px;
    classDef yaw fill:#e8f5e9,stroke:#2e7d32,stroke-width:2px;
    classDef action fill:#ffffff,stroke:#333,stroke-width:1px,stroke-dasharray: 5 5;

    %% 1. Thrust (추력)
    subgraph S_Thrust [1. Thrust (상승)]
        direction TB
        T[<b>Thrust (+)</b><br/>상승 명령]:::thrust
        T_Act["모든 모터 속도 증가<br/>(M1, M2, M3, M4 : +1)"]:::action
        T_Res(🚀 수직 상승):::thrust
        
        T --> T_Act --> T_Res
    end

    %% 2. Roll (롤/좌우)
    subgraph S_Roll [2. Roll (좌우 기울기)]
        direction TB
        R[<b>Roll (+)</b><br/>우측 이동 명령]:::roll
        R_Act["좌측(M2,M3): <b>+1 (상승)</b><br/>우측(M1,M4): <b>-1 (하강)</b>"]:::action
        R_Res(↘️ 우측으로 기울어짐):::roll
        
        R --> R_Act --> R_Res
    end

    %% 3. Pitch (피치/전후)
    subgraph S_Pitch [3. Pitch (전후 기울기)]
        direction TB
        P[<b>Pitch (+)</b><br/>전진 이동 명령]:::pitch
        P_Act["후방(M2,M4): <b>+1 (상승)</b><br/>전방(M1,M3): <b>-1 (하강)</b>"]:::action
        P_Res(↘️ 앞으로 기울어짐):::pitch
        
        P --> P_Act --> P_Res
    end

    %% 4. Yaw (요/회전)
    subgraph S_Yaw [4. Yaw (제자리 회전)]
        direction TB
        Y[<b>Yaw (+)</b><br/>우회전(CW) 명령]:::yaw
        Y_Act["CW모터(M1,M4): <b>+1 (가속)</b><br/>CCW모터(M2,M3): <b>-1 (감속)</b>"]:::action
        Y_Res(↻ 시계방향 회전<br/>반작용 토크 원리):::yaw
        
        Y --> Y_Act --> Y_Res
    end
